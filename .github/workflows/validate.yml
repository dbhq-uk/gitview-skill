name: Validate

on:
  push:
  pull_request:

# The default for every job below. A job that needs more declares it itself;
# an explicit floor here means a new job cannot silently inherit an org
# setting somebody widened.
permissions:
  contents: read

jobs:
  # THE ESTATE FLOOR, defined once in dbhq-uk/.github (dbhq-uk/.github#4):
  # SKILL.md frontmatter parses as YAML and names its directory, plugin.json
  # parses and carries no version, skill paths are braced (gitview shipped
  # the unbraced form from its first commit), every shell script passes
  # bash -n and shellcheck, ruff E9,F, and no em or en dashes. Pinned by SHA;
  # Dependabot moves the pin.
  floor:
    uses: dbhq-uk/.github/.github/workflows/skill-floor.yml@af131df509f44eb9abfe8b37ad6299c11f496c5f
    with:
      # The Python is held to the dash rule too, as it was before the floor.
      dash-files: "*.md *.py"

  validate:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7.0.1

      - name: Nothing client-specific reached this public repository
        run: |
          if grep -rInE '[0-9]{1,3}(\.[0-9]{1,3}){3}|[A-Z]{3,}-[0-9]{3,}|dev\.azure\.com/[A-Za-z]' \
               --include='*.md' --include='*.py' --include='*.yml' \
               --exclude-dir=.github . ; then
            echo "That looks like an IP address, a ticket id, or a real organisation. Check it."
            exit 1
          fi

      - name: Tests
        run: |
          python3 -m pip install --quiet pytest
          python3 -m pytest skills/gitview/tests -q
